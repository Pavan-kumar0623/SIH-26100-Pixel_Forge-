{
  "name": "procure-ai-platform",
  "version": "1.0.0",
  "description": "ProcureAI — Enterprise AI Procurement & Vigilance Platform",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue.bold,cyan.bold\" \"npm run backend\" \"npm run frontend\"",
    "dev:all": "concurrently -n \"OCR,BACKEND,FRONTEND\" -c \"magenta.bold,blue.bold,cyan.bold\" \"npm run ocr\" \"npm run backend\" \"npm run frontend\"",
    "backend": "node run-python.js -m uvicorn main:app --port 8000 --reload",
    "frontend": "cd frontend && npm run dev",
    "seed": "node run-python.js seed.py",
    "test:backend": "node run-python.js -m pytest tests/ -v",
    "ocr": "cd ocr_worker && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
